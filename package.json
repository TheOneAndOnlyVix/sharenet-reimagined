{
  "name": "sharenet-ai",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for ShareNet AI Assistant",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
